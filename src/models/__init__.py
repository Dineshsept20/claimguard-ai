"""Machine learning models for claims anomaly detection.

Includes Isolation Forest (unsupervised), XGBoost (supervised),
and a hybrid ensemble combining both approaches with domain-specific
business rules.
"""
